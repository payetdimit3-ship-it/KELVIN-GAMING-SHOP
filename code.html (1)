<!DOCTYPE html>
<html lang="sw">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin — GameHub</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <header class="navbar">
    <div class="logo">🎮 <span>GAME</span>HUB <span style="font-size:12px;color:#ff3d71;background:rgba(255,61,113,0.15);padding:2px 8px;border-radius:6px;margin-left:6px;">ADMIN</span></div>
    <nav class="nav-links">
      <a href="index.html">🏠 Home</a>
      <a href="shop.html">🛒 Shop</a>
      <a href="account.html">👤 Account</a>
      <a href="admin.html" style="color:#00e5ff;">⚙️ Admin</a>
    </nav>
  </header>

  <div class="admin-page" id="adminPage">
    <h1>⚙️ Admin Dashboard</h1>

    <!-- Stats -->
    <div class="admin-stats">
      <div class="stat-card"><h3 id="statOrders">0</h3><p>📦 Mauzo Yote</p></div>
      <div class="stat-card"><h3 id="statRevenue">0 TZS</h3><p>💰 Mapato</p></div>
      <div class="stat-card"><h3 id="statUsers">0</h3><p>👥 Wateja</p></div>
      <div class="stat-card"><h3 id="statProducts">0</h3><p>🎮 Bidhaa</p></div>
    </div>

    <!-- Tabs -->
    <div class="admin-tabs">
      <button class="tab-btn active" data-tab="orders">📦 Mauzo</button>
      <button class="tab-btn" data-tab="products">🎮 Bidhaa</button>
      <button class="tab-btn" data-tab="users">👥 Wateja</button>
      <button class="tab-btn" data-tab="ai">🤖 AI Admin</button>
      <button class="tab-btn" data-tab="requests">🔍 Ombi</button>
      <button class="tab-btn" data-tab="security">🛡️ Usalama</button>
    </div>

    <!-- 📦 ORDERS -->
    <div class="tab-content" id="tab-orders">
      <h2>📦 Mauzo Yote</h2>
      <div class="admin-list" id="ordersList"><p style="color:#8f8faa;">Inapakia...</p></div>
    </div>

    <!-- 🎮 PRODUCTS -->
    <div class="tab-content" id="tab-products" style="display:none;">
      <h2>🎮 Bidhaa Zote</h2>
      <div class="admin-list" id="productsList"><p style="color:#8f8faa;">Inapakia...</p></div>

      <div class="form-card">
        <h3>➕ Ongeza Bidhaa Mpya</h3>
        <div class="form-group">
          <label>Jina la bidhaa</label>
          <input type="text" id="addName" placeholder="mf: FIFA 25">
        </div>
        <div class="form-row">
          <div class="form-group">
            <label>Aina</label>
            <select id="addType">
              <option value="game">🎮 Game</option>
              <option value="account">👤 Steam Account</option>
              <option value="topup">💎 Top-Up</option>
            </select>
          </div>
          <div class="form-group">
            <label>Bei (TZS)</label>
            <input type="number" id="addPrice" placeholder="45000">
          </div>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label>Emoji</label>
            <input type="text" id="addEmoji" value="🎮">
          </div>
          <div class="form-group">
            <label>Link (optional)</label>
            <input type="text" id="addLink" placeholder="https://...">
          </div>
        </div>
        <div class="form-group">
          <label>Maelezo</label>
          <textarea id="addDesc" placeholder="Maelezo mafupi ya bidhaa..."></textarea>
        </div>
        <button class="btn-primary" onclick="addNewProduct()">➕ Ongeza Bidhaa</button>
        <span id="addResult" style="margin-left:10px;font-size:13px;"></span>
      </div>
    </div>

    <!-- 👥 USERS -->
    <div class="tab-content" id="tab-users" style="display:none;">
      <h2>👥 Wateja Wote</h2>
      <div class="admin-list" id="usersList"><p style="color:#8f8faa;">Inapakia...</p></div>
    </div>

    <!-- 🤖 AI ADMIN -->
    <div class="tab-content" id="tab-ai" style="display:none;">
      <h2>🤖 AI Admin — Amri kwa Maneno</h2>
      <p style="color:#8f8faa;margin-bottom:10px;">Andika amri kama: "ongeza game FIFA 26 bei 50000" au "futa bidhaa FIFA" au "mauzo" au "wateja"</p>
      <div class="chat-messages" id="adminChatMessages" style="background:var(--card);border:1px solid var(--border);border-radius:12px;padding:14px;max-height:250px;overflow-y:auto;margin-bottom:12px;">
        <div class="chat-msg ai" style="background:rgba(0,229,255,0.08);padding:8px 12px;border-radius:10px;font-size:13px;margin-bottom:8px;">👋 Karibu Admin! Taja amri kwa maneno.</div>
      </div>
      <div style="display:flex;gap:8px;">
        <input type="text" id="adminChatInput" placeholder="ongeza game... futa bidhaa..." style="flex:1;background:var(--card);border:1px solid var(--border);border-radius:10px;padding:12px;color:#fff;font-size:14px;" onkeydown="if(event.key==='Enter')sendAdminChat()">
        <button class="btn-primary" onclick="sendAdminChat()">Tuma</button>
      </div>
    </div>

    <!-- 🔍 REQUESTS -->
    <div class="tab-content" id="tab-requests" style="display:none;">
      <h2>🔍 Ombio za Wateja</h2>
      <div class="admin-list" id="requestsList"><p style="color:#8f8faa;">Inapakia...</p></div>
    </div>

    <!-- 🛡️ SECURITY -->
    <div class="tab-content" id="tab-security" style="display:none;">
      <h2>🛡️ Usalama</h2>
      <div class="admin-list" id="securityList"><p style="color:#8f8faa;">Inapakia...</p></div>
      <div class="form-card">
        <h3>🔒 Fungua IP</h3>
        <div class="form-row">
          <div class="form-group">
            <input type="text" id="blockIp" placeholder="IP address">
          </div>
          <div class="form-group">
            <input type="text" id="blockReason" placeholder="Sababu">
          </div>
        </div>
        <button class="btn-primary" onclick="blockIP()">🔒 Fungua IP</button>
        <span id="blockResult" style="margin-left:10px;font-size:13px;"></span>
      </div>
    </div>
  </div>

  <script>
    const token = localStorage.getItem('gamehubToken');
    const user = JSON.parse(localStorage.getItem('gamehubUser') || 'null');

    // Redirect if not admin
    if (!user || !user.isAdmin) {
      document.getElementById('adminPage').innerHTML = '<div class="cart-page" style="text-align:center;padding:60px;"><div style="font-size:48px;">🚫</div><h2 style="margin:15px 0;">Wewe si Admin</h2><p style="color:#8f8faa;">Sehemu hii ni kwa ajili ya admin pekee.</p><a href="login.html" class="btn-primary" style="margin-top:15px;display:inline-block;">Ingia kama Admin</a></div>';
    } else {
      loadAll();
    }

    // ===== LOAD ALL =====
    async function loadAll() {
      await loadOrders();
      await loadProducts();
      await loadUsers();
      await loadRequests();
      await loadSecurity();
    }

    // ===== ORDERS =====
    async function loadOrders() {
      try {
        const res = await fetch('/api/orders', { headers: { 'Authorization': token } });
        const data = await res.json();
        const orders = data.orders || data || [];
        document.getElementById('statOrders').textContent = orders.length;
        const revenue = orders.reduce((s, o) => s + (o.amount || 0), 0);
        document.getElementById('statRevenue').textContent = revenue.toLocaleString() + ' TZS';
        if (!orders.length) {
          document.getElementById('ordersList').innerHTML = '<p style="color:#8f8faa;">Hakuna mauzo bado.</p>';
          return;
        }
        document.getElementById('ordersList').innerHTML = orders.slice(0, 30).map(o => `
          <div class="admin-row">
            <div>
              <strong>${o.items ? (typeof o.items === 'string' ? JSON.parse(o.items).map(i => i.name).join(', ') : (Array.isArray(o.items) ? o.items.map(i => i.name).join(', ') : o.items)) : '—'}</strong><br>
              <small>${o.customer} • ${o.phone || ''} • ${new Date(o.created_at || o.date).toLocaleString()}</small><br>
              <small style="color:${o.status === 'paid' ? '#00ff88' : '#ffb020'};">${o.status === 'paid' ? '✅ Imelipwa' : '⏳ Inasubiri'}</small>
              <small> • Ref: ${o.tx_ref}</small>
            </div>
            <div class="admin-amount">${Number(o.amount).toLocaleString()} TZS</div>
          </div>
        `).join('');
      } catch(e) { document.getElementById('ordersList').innerHTML = '<p style="color:#ff3d71;">Hitilafu.</p>'; }
    }

    // ===== PRODUCTS =====
    async function loadProducts() {
      try {
        const products = await getProducts();
        document.getElementById('statProducts').textContent = products.length;
        const list = document.getElementById('productsList');
        if (!products.length) { list.innerHTML = '<p style="color:#8f8faa;">Hakuna bidhaa.</p>'; return; }
        list.innerHTML = products.map(p => `
          <div class="admin-row">
            <div>${p.emoji || '🎮'} <strong>${p.name}</strong> <span style="color:#8f8faa;font-size:12px;">(${p.type || 'game'})</span></div>
            <div style="display:flex;align-items:center;gap:10px;">
              <span class="admin-amount">${Number(p.price).toLocaleString()} TZS</span>
              <button onclick="deleteProduct(${p.id})" style="background:rgba(255,61,113,0.15);border:none;color:#ff3d71;border-radius:8px;padding:4px 10px;cursor:pointer;font-size:12px;">🗑️</button>
            </div>
          </div>
        `).join('');
      } catch(e) {}
    }

    async function addNewProduct() {
      const name = document.getElementById('addName').value.trim();
      const price = document.getElementById('addPrice').value;
      const type = document.getElementById('addType').value;
      const emoji = document.getElementById('addEmoji').value.trim() || '🎮';
      const link = document.getElementById('addLink').value.trim();
      const desc = document.getElementById('addDesc').value.trim();
      const result = document.getElementById('addResult');
      if (!name || !price) { result.textContent = '⚠️ Jaza jina na bei.'; result.style.color = '#ff3d71'; return; }
      result.textContent = '⏳ Inaongeza...'; result.style.color = '#8f8faa';
      const res = await addProduct(name, type, price, emoji, desc, link);
      if (res.success) {
        result.textContent = `✅ "${name}" imeongezwa!`;
        result.style.color = '#00ff88';
        document.getElementById('addName').value = '';
        document.getElementById('addPrice').value = '';
        document.getElementById('addDesc').value = '';
        document.getElementById('addLink').value = '';
        await loadProducts();
      } else {
        result.textContent = '❌ Hitilafu: ' + (res.error || 'Jaribu tena');
        result.style.color = '#ff3d71';
      }
    }

    async function deleteProduct(id) {
      if (!confirm('Una uhakika unataka kufuta bidhaa hii?')) return;
      await deleteProduct(id);
      await loadProducts();
    }

    // ===== USERS =====
    async function loadUsers() {
      try {
        const res = await fetch('/api/users', { headers: { 'Authorization': token } });
        const users = await res.json();
        document.getElementById('statUsers').textContent = users.length || 0;
        const list = document.getElementById('usersList');
        if (!users.length) { list.innerHTML = '<p style="color:#8f8faa;">Hakuna wateja.</p>'; return; }
        list.innerHTML = users.map(u => `
          <div class="admin-row">
            <div><strong>${u.name}</strong> • ${u.email} ${u.phone ? '• ' + u.phone : ''}</div>
            <div style="font-size:12px;color:#8f8faa;">${u.created ? new Date(u.created).toLocaleDateString() : '—'}</div>
          </div>
        `).join('');
      } catch(e) {}
    }

    // ===== AI ADMIN =====
    async function sendAdminChat() {
      const input = document.getElementById('adminChatInput');
      const msg = input.value.trim();
      if (!msg) return;
      input.value = '';
      const msgs = document.getElementById('adminChatMessages');
      msgs.innerHTML += `<div class="chat-msg user" style="background:rgba(123,47,247,0.2);padding:8px 12px;border-radius:10px;font-size:13px;margin-bottom:8px;margin-left:auto;max-width:85%;">${msg}</div>`;
      msgs.innerHTML += `<div class="chat-msg ai" style="color:#8f8faa;padding:8px 12px;font-size:13px;">✍️ Inatuma...</div>`;
      msgs.scrollTop = msgs.scrollHeight;

      try {
        const res = await fetch('/api/ai/chat', {
          method: 'POST', headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ message: msg, isAdmin: true })
        });
        const data = await res.json();
        msgs.innerHTML = msgs.innerHTML.replace('<div class="chat-msg ai" style="color:#8f8faa;padding:8px 12px;font-size:13px;">✍️ Inatuma...</div>', '');
        msgs.innerHTML += `<div class="chat-msg ai" style="background:rgba(0,229,255,0.08);padding:8px 12px;border-radius:10px;font-size:13px;margin-bottom:8px;">${data.reply}</div>`;
        if (data.action === 'reload') { await loadProducts(); await loadOrders(); }
      } catch(e) {
        msgs.innerHTML = msgs.innerHTML.replace('<div class="chat-msg ai" style="color:#8f8faa;padding:8px 12px;font-size:13px;">✍️ Inatuma...</div>', '');
        msgs.innerHTML += `<div class="chat-msg ai" style="background:rgba(0,229,255,0.08);padding:8px 12px;border-radius:10px;font-size:13px;margin-bottom:8px;">Hitilafu.</div>`;
      }
      msgs.scrollTop = msgs.scrollHeight;
    }

    // ===== REQUESTS =====
    async function loadRequests() {
      try {
        const requests = await getRequests();
        const list = document.getElementById('requestsList');
        if (!requests.length) { list.innerHTML = '<p style="color:#8f8faa;">Hakuna ombi.</p>'; return; }
        list.innerHTML = requests.map(r => `
          <div class="request-item">
            <div><strong>${r.name}</strong> <span class="votes">${r.votes} 👍</span></div>
            <button onclick="approveRequest(${r.id}, '${r.name.replace(/'/g, "\\'")}')" style="background:rgba(0,255,136,0.15);border:none;color:#00ff88;padding:6px 12px;border-radius:8px;cursor:pointer;">➕ Ongeza Dukani</button>
          </div>
        `).join('');
      } catch(e) {}
    }

    async function approveRequest(id, name) {
      const res = await fetch(`/api/requests/approve/${id}`, { method: 'POST', headers: { 'Authorization': token } });
      const data = await res.json();
      if (data.success) {
        await loadRequests();
        await loadProducts();
        alert(data.message);
      } else {
        alert('Hitilafu: ' + (data.error || 'Jaribu tena'));
      }
    }

    // ===== SECURITY =====
    async function loadSecurity() {
      try {
        const logs = await getLogs();
        const list = document.getElementById('securityList');
        if (!logs.length) { list.innerHTML = '<p style="color:#00ff88;">✅ Hakuna matukio ya usalama.</p>'; return; }
        list.innerHTML = logs.slice(0, 20).map(l => `
          <div class="admin-row">
            <div>
              <strong>${l.type}</strong><br>
              <small>${l.details || ''}</small><br>
              <small style="color:#8f8faa;">${l.ip ? 'IP: ' + l.ip : ''} • ${new Date(l.time || l.created_at).toLocaleString()}</small>
            </div>
            <div style="color:${l.severity === 'high' ? '#ff3d71' : l.severity === 'medium' ? '#ffb020' : '#8f8faa'};font-size:12px;">${l.severity}</div>
          </div>
        `).join('');
      } catch(e) { document.getElementById('securityList').innerHTML = '<p style="color:#8f8faa;">Hitilafu.</p>'; }
    }

    async function blockIP() {
      const ip = document.getElementById('blockIp').value.trim();
      const reason = document.getElementById('blockReason').value.trim();
      const result = document.getElementById('blockResult');
      if (!ip) { result.textContent = '⚠️ Weka IP.'; result.style.color = '#ff3d71'; return; }
      const res = await fetch('/api/security/block', {
        method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': token },
        body: JSON.stringify({ ip, reason: reason || 'Admin block' })
      });
      const data = await res.json();
      if (data.success) {
        result.textContent = `✅ IP ${ip} imefungwa.`;
        result.style.color = '#00ff88';
        document.getElementById('blockIp').value = '';
        document.getElementById('blockReason').value = '';
      } else {
        result.textContent = '❌ Hitilafu.';
        result.style.color = '#ff3d71';
      }
    }

    // ===== TABS =====
    document.querySelectorAll('.tab-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        document.querySelectorAll('.tab-content').forEach(t => t.style.display = 'none');
        document.getElementById('tab-' + btn.dataset.tab).style.display = 'block';
      });
    });
  </script>
  <script src="https://unpkg.com/@supabase/supabase-js@2"></script>
  <script src="supabase.js"></script>
</body>
</html>
